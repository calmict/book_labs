# cap09 start - a Dockerfile with the fundamental instructions, to complete.
# Three gaps (TODO 1..3). As written the image builds but does not load the app,
# does not set the greeting and has no default command: the test fails.
FROM busybox
WORKDIR /app
RUN echo "built" > /app/built.txt

# TODO 1 (9.3): COPY the app script from the build context into the WORKDIR:
#     COPY greet.sh /app/greet.sh

# TODO 2 (9.4): set the default greeting as an environment variable:
#     ENV GREETING=ciao

# TODO 3 (9.5): declare the default command that runs the app at runtime:
#     CMD ["sh", "/app/greet.sh"]
