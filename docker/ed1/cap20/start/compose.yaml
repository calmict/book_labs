# cap20 start - a two-service app, to complete. Fill TODO 1..3. As written the
# services have no long-running command, so their containers exit at once and the
# test fails.
services:
  db:
    image: busybox
    # TODO 1 (20.2): give db a long-running command so it stays up:
    #   command: sleep 3600
  web:
    image: busybox
    # TODO 2 (20.2): give web a long-running command so it stays up:
    #   command: sleep 3600
    # TODO 3 (20.3): make web start after db:
    #   depends_on:
    #     - db
