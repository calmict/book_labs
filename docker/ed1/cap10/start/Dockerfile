# cap10 start - ENTRYPOINT vs CMD, to complete. Three gaps (TODO 1..3). As
# written the image is just busybox: no script, no captain, no default orders,
# so the test fails.
FROM busybox

# TODO 1 (10.3): COPY the entrypoint script into the image (it is executable in
#   the build context, and COPY preserves the mode):
#     COPY entry.sh /entry.sh

# TODO 2 (10.3): name the fixed executable in EXEC form (JSON array), so the
#   script is the startup process and is PID 1:
#     ENTRYPOINT ["/entry.sh"]

# TODO 3 (10.5): give ENTRYPOINT default arguments with CMD - overridable at run:
#     CMD ["default"]
