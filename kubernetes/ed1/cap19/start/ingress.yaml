# The written request: two hosts, one door, each to its own tenant.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: labs
spec:
  ingressClassName: nginx
  rules: []
  # TODO: replace the empty rules with two host-based entries:
  # - host uno.labs.local -> path / (Prefix) -> service uno, port 80
  # - host due.labs.local -> path / (Prefix) -> service due, port 80
