# Three lines that invert the world: every pod selected, Ingress ruled,
# no rules = no entry. Read it, then apply it.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all
  namespace: vault
spec:
  podSelector: {}
  policyTypes: ["Ingress"]
