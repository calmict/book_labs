# The vault does not make phone calls: whoever cracks it carries
# nothing out.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: no-exfiltration
  namespace: vault
spec:
  # TODO: select the safe again, but this time rule Egress — with no
  # egress rules at all (silence is the deny)
