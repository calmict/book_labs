# Chapter 13 — Answers

## The timeline of the relay

    # paste here the events with the four signatures, then write the
    # LOGICAL order (timestamps only have one-second precision)

## The chain of ownership

    # paste here: pod -> replicaset -> deployment from the ownerReferences

## Below the API

    # paste here the PID, its /proc/<pid>/cgroup line (mark kubepods and
    # the QoS class) and the pid namespace inode

## The two cures

    # paste here the pod line with RESTARTS=1 after the kill -9,
    # and the new pod name after the delete

## The three questions

**a. The relay timeline: map each signatory to its chapter, and give the
logical order beyond the timestamps.**

_(your answer)_

**b. The two cures: who acted in each case, which signal gives it away,
and why are both levels of healing needed?**

_(your answer)_

**c. What are ownerReferences for, and what do you expect if you delete
the ReplicaSet instead of the Pod?**

_(your answer)_
