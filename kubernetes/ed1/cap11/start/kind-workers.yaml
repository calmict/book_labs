# A control plane and two workers: enough geography to steer a scheduler.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: book-labs-sched
nodes:
  - role: control-plane
  - role: worker
  - role: worker
