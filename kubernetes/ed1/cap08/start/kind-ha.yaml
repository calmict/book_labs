# 3 control-plane nodes = 3 etcd members: the minimum for a real quorum.
# kind also adds a load balancer in front of the three apiservers.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: book-labs-ha
nodes:
  - role: control-plane
  - role: control-plane
  - role: control-plane
