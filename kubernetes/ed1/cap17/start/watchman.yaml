# Note what is MISSING: no replicas, anywhere — and that is not a TODO.
# The cluster's geography decides the count.
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: watchman
spec:
  selector:
    matchLabels:
      app: watchman
  template:
    metadata:
      labels:
        app: watchman
    spec:
      # TODO: one container named watchman, image alpine:3, sleep infinity
      # (later, step 2 will ask you to add the control-plane toleration
      # here — chapter 11 knows which one)
