# Apply this LAST: the before (403) and after (the list) is the lesson.
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: robot-pod-reader
  namespace: default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: pod-reader
subjects: []
# TODO: one subject — kind ServiceAccount, name robot, namespace default
# (note: no apiGroup here — ServiceAccounts are core objects)
