[defaults]
inventory = inventory.ini

# TODO 1 (25.2) - forks: how many nodes Ansible drives at once.
# The fleet has 12 nodes but this is set low, so Ansible works in several waves
# and the run takes far longer than the slowest node. Raise it so the whole
# fleet runs in a single wave.
forks = 4
