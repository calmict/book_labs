# Chapter 7 — your project rulebook (starting point).
# INI structure: sections in square brackets, key = value lines.
# This file is picked up because it sits in the current directory (the second
# strongest of the four stands). Check with:  ansible --version | grep 'config file'

[defaults]
# TODO 1: complete the three core settings.
#   inventory = ./inventory.ini      <- the address book (chapter 8); stop passing -i
#   forks = 10                       <- parallel hosts (default is only 5)
#   host_key_checking = False        <- lab only! (chapter 3); NEVER in production

[privilege_escalation]
# TODO 2a: make privilege escalation the default for this project.
#   become = True
#   become_method = sudo

[ssh_connection]
# TODO 2b: fewer SSH round-trips per task (the sibling of chapter 3's ControlMaster).
#   pipelining = True
