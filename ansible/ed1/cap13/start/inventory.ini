# Chapter 13 — two hosts, connection local. Precedence is resolved on the
# control node (before anything touches a machine), so this chapter needs no
# SSH and no managed nodes: web1 and web2 are just names to attach variables to.
[web]
web1
web2

[all:vars]
ansible_connection=local
