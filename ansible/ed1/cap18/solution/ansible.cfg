[defaults]
inventory = ./inventory.ini
host_key_checking = False

# One of the three ways (18.6) to supply the vault password: point at a file so
# you do not type it on every run. The password file itself is NEVER committed.
# vault_password_file = ./vpass.txt
