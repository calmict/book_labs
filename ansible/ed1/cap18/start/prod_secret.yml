---
# TODO 3 - a production secret under its OWN vault-id (18.7). Encrypt THIS file
# with a label of its own and a separate password:
#     ansible-vault encrypt --encrypt-vault-id prod --vault-id prod@prompt prod_secret.yml
# The header becomes  $ANSIBLE_VAULT;1.2;AES256;prod  (note the 1.2 format and
# the 'prod' label). Then run prod.yml passing BOTH identities:
#     ansible-playbook -i inventory.ini prod.yml \
#         --vault-id lab@vpass.txt --vault-id prod@prod-pass.txt
prod_db_password: prod-DB-pw
