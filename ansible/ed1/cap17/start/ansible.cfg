[defaults]
# Chapter 17 — keep collections WITH the project, not in the global
# ~/.ansible/collections, so anyone who clones the repo gets the same versions.
#
# TODO 2: point the collections path at a folder inside the project:
#   collections_path = ./collections
