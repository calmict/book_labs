# Chapter 11 — Answers

## Inside the notebook (Phase 1)

    # paste: serial, lineage, and the container's real id from the state

    # paste the container's dependencies entry:

## The secret (Phase 2)

    # paste side by side: the output line (<sensitive>) and the first
    # characters of the plaintext result from the state

## The three sources (Phase 3)

    # paste the -refresh-only header lines (changed outside / has been
    # deleted):

    # after apply -refresh-only: paste tofu state list

## The colleague (Phase 4)

    # his plan line:
    # his apply error (first Error line):
    # his state list after the crash:

## The three questions

**a. What the notebook knows.**

_(4-6 lines: the binding neither code nor reality contains, and why
dependencies are recorded too)_

**b. The secret and the memory-only sync.**

_(4-6 lines: why the value must be in the state, the operational
consequences, and when -refresh-only is the right tool)_

**c. The colleague and the single notebook.**

_(4-6 lines: the incident via the three sources, why discipline does not
scale, what chapter 12's shared state must guarantee — including the lock)_
