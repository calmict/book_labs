# Chapter 16 — Answers

## The bench (Phase 0)

    # paste what console returned for:
    #   lower(trimspace("  Web-01 "))
    #   split("-", "web-01")[0]

## The assembly line (Phase 1)

    # after TODO 1, tofu state list -> paste the container addresses
    # (4 raw hosts, how many containers?):

## The derived map and the filter (Phases 2-3)

    # tofu output host_roles:
    # tofu output web_hosts:

## The three questions

**a. The bench.**

_(3-5 lines: what an HCL function is — input/output/side effects; why console is
safe on production; three functions and their families)_

**b. The assembly line.**

_(3-5 lines: list vs map comprehension, what the trailing if does; toset()'s
role vs the for alone; why 4 raw hosts gave 3 containers)_

**c. Try first, fit later.**

_(3-5 lines: why console before code; the link to chapter 6's plan/apply; what
the bench saves and how it differs from a plan)_
